---
const { items = [] } = Astro.props;
---

<div class="mt-3 flex flex-wrap gap-2">
  {items.map((item) => (
    <span
      class="rounded-full border border-cyan-200/30 bg-slate-950/70 px-3 py-1 text-xs font-medium text-cyan-100/90 shadow-[0_8px_24px_rgba(2,6,20,0.35)]"
    >
      {item}
    </span>
  ))}
</div>
